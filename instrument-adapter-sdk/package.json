{
  "name": "newgen-adapter-sdk",
  "version": "1.0.0-alpha",
  "description": "Instrument Adapter SDK + OPC-UA Simulator for NewGen Studio",
  "private": true,
  "workspaces": [
    "adapter-sdk",
    "simulator"
  ],
  "scripts": {
    "test": "npm -w adapter-sdk run test",
    "test:integration": "npm -w adapter-sdk run test:integration",
    "build": "npm -w adapter-sdk run build && npm -w simulator run build",
    "dev": "concurrently \"npm -w simulator start\" \"npm -w adapter-sdk run dev\"",
    "clean": "npm -w adapter-sdk run clean && npm -w simulator run clean"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
